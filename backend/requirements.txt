# Python 3.14 compatible versions.
# pydantic>=2.11.0 ships pre-built wheels for Python 3.14 — no Rust compilation needed.
# Earlier versions (e.g. 2.7.4) had no 3.14 wheel and fail to compile on Windows.

# Core
fastapi==0.115.12
uvicorn[standard]==0.34.3

# OpenAI SDK v1.x (AsyncOpenAI client)
openai==1.84.0

# Pydantic v2 — 2.11.x has Python 3.14 pre-built wheels
pydantic==2.11.5
pydantic-settings==2.9.1

# HTTP client used internally by openai SDK
httpx==0.28.1

# Environment variable loading
python-dotenv==1.1.0

# Async support
anyio==4.9.0
